type: api
account: admin@music.com / Admin123456!
tool: Postman — POST /api/songs/{id}/like, Bearer {{tokenAdmin}}
steps:
  1. Gọi POST /api/songs/{id}/like ngay sau AC-1 (cùng bài, đã like)
expected: HTTP 200, { "liked": false, "newCount": <số like mới> }
screenshot: Response body JSON
